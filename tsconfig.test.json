{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["jest", "node"],
    "rootDir": "."   // <--- add or override rootDir for test config
  },
  "include": [
    "src",
    "tests"
  ]
}